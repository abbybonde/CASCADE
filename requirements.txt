# CASCADE — fitting_gui.py dependencies
#
# Install with:
#   pip install -r requirements.txt
#
# PyTorch is the only non-standard scientific dependency.
# TensorFlow is NOT used. CUDA is NOT required — CPU-only PyTorch works
# for all fitting operations in the GUI.
#
# Install PyTorch separately because the correct wheel depends on your platform:
#
#   CPU-only (recommended for the GUI, smallest download ~250 MB):
#     pip install torch --index-url https://download.pytorch.org/whl/cpu
#
#   CUDA 12.x (if you want GPU acceleration for large batch fits):
#     pip install torch --index-url https://download.pytorch.org/whl/cu121
#
#   Apple Silicon (MPS acceleration included in the default wheel):
#     pip install torch

# ── Core numerical stack ──────────────────────────────────────────────────────
numpy>=1.24
scipy>=1.10           # voigt_peak (scipy.special.wofz), linear_sum_assignment

# ── Plotting (TkAgg backend ships with matplotlib) ───────────────────────────
matplotlib>=3.7
pandas>=1.5           # used in plot_utils._plot_sweep

# ── HDF5 I/O ─────────────────────────────────────────────────────────────────
h5py>=3.8
lazy5>=0.2            # load_h5_file / save_h5_file in dataset_utils

# ── GUI toolkit ───────────────────────────────────────────────────────────────
# tkinter ships with the standard CPython distribution on Windows and macOS.
# On Debian/Ubuntu Linux install the system package if it is missing:
#   sudo apt install python3-tk

# ── Deep learning framework (install separately — see note above) ─────────────
# torch>=2.0
